#! /usr/bin/env bash
#
# Backward-compatibility wrapper forwarding to the new name of the script.
#
cat <<EOF
Note: The "bro-pkg-bundle" tool has been renamed to "zkg-bundle". Use of the old
      name is deprecated and support will be removed in a future version.
EOF

exec $(dirname $0)/zkg-bundle $@
